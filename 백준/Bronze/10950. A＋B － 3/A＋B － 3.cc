#include <iostream>
using namespace std;
int main() {
    ios::sync_with_stdio(false);
    int TestCase;
    cin >> TestCase;
    while(TestCase--){
        int a,b;
        cin >> a >> b;
        cout << a+b << "\n";
    }
}